package mdlink

import io.circe.syntax.*
import io.circe.parser.*
import io.circe.generic.auto.*
import io.circe.DecodingFailure

class utilSuite extends CommonSuite:
  test("example: ") {
    assert(true)
  }
